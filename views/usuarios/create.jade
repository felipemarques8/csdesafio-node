extends ../layout

block content
	h1 Cadastro de Usuários
	hr
	br
	if messages.erro
		div(class="alert alert-danger")
			each msg in messages.erro
				b= msg
				br
		br
	form(method="POST", action="/usuarios/create", role="form")
		div(class="form-group")
			label Nome
			input(type="text", class="form-control", name="nome", value="#{user.nome || ''}" placeholder="Nome", autofocus, required)
		div(class="form-group")
			label E-mail
			input(type="email", class="form-control", name="email", value="#{user.email || ''}" placeholder="E-mail", required)
		div(class="form-group")
			label Telefone
			input(type="text", class="form-control", name="telefone", value="#{user.telefone || ''}" placeholder="Telefone")
		div(class="form-group")
			label Senha
			input(type="password", class="form-control", name="password", placeholder="Senha", required)
		div(class="form-group")
			label Confirmar Senha
			input(type="password", class="form-control", name="password_confirmar", placeholder="Confirmar Senha", required)
		button(type="submit", class="btn btn-success") Salvar
	br
	br
	a(href="/usuarios") Voltar